{
  "name": "parcel-resolver-typescript-esm",
  "version": "1.0.0",
  "description": "Parcel v2 plugin for TypeScript ESM imports.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/b8kkyn/parcel-resolver-typescript-esm"
  },
  "source": "src/index.ts",
  "main": "dist/index.js",
  "packageManager": "yarn@3.2.3",
  "scripts": {
    "watch": "yarn clean && tsc --watch",
    "build": "yarn lint && yarn clean && tsc",
    "clean": "rimraf dist",
    "lint": "ts-standard"
  },
  "engines": {
    "node": ">= 12.0.0",
    "parcel": ">= 2.5.0"
  },
  "dependencies": {
    "@parcel/plugin": "^2.5.0"
  },
  "peerDependencies": {
    "parcel": "^2.5.0"
  },
  "devDependencies": {
    "@parcel/types": "^2.7.0",
    "@tsconfig/node12": "^1.0.11",
    "@types/node": "^18.8.3",
    "rimraf": "^3.0.2",
    "ts-standard": "^11.0.0",
    "typescript": "^4.4.4"
  }
}
